export class User{
    constructor( public id:number, public role: string, public firstName:string, public lastName:string, public password:string, public gender: string, public mobileNumber: number, public age: number, vote: string, public date: string, public email: string, public village: string, public address: string){}
}